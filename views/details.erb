<h4>podrobnosty posta:</h4>
<br />
<p>
	<%= @row['content'] %>
	
 Added at <%= @row['created_date'] %>
 </p>
 
<h4>Comments:</h4>
<br />
<% @comments.each do |comment| %>
	<p>
		<%= comment['content'] %>
		
		<span style="font-size: 50%; color: #999">Added at <%= comment['created_date'] %></span>
	</p>
	<hr />
<% end %>
<p>
	<form action="/details/<%= @row['id'] %>" method="post">
	<textarea name="content" class="form-control" rows="6" placeholder="otvette"></textarea>
	
	  <button type="submit" class="btn btn-primary">dobavit comment</button>
	</form>
</p>