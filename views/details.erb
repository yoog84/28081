<h4>podrobnosty posta:</h4>
<br />
<p>
	<%= @row['111'] %>
	
 Added at <%= @row['created_date'] %>
 </p>
 
<h4>Comments:</h4>
<br />
<% @comments.each do |comment| %>
	<p>
		<%= comment['111'] %>
		
		<span style="font-size: 50%; color: #444">Added at <%= comment['created_date'] %></span>
	</p>
	<hr />
<% end %>
<p>
	<form action="/details/<%= @row['id'] %>" method="post">
	<textarea name="111" class="form-control" rows="6" placeholder="otvette"></textarea>
	
	  <button type="submit" class="btn btn-default">dobavit comment</button>
	</form>
</p>